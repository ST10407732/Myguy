@* @model IEnumerable<MYGUYY.Models.UserTask>

<h2>View Requests</h2>

<table class="table">
    <thead>
        <tr>
            <th>Request ID</th>
            <th>Status</th>
            <th>Client</th>
            <th>Driver</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var request in Model)
        {
            <tr>
                <td>@request.Id</td>
              @*   <td>@request.Status</td> *@
               @*  <td>@request.Client.Name</td>
                <td>@request.Driver?.Name ?? "Not Assigned"</td> *@
                <td>
                   @*  @if (request.Status == "Pending" *@)
                 @*    {
                        <a href="@Url.Action("AssignDriver", "Admin", new { id = request.Id })">Assign Driver</a>
                    }
                    <a href="@Url.Action("ViewDetails", "Admin", new { id = request.Id })">View Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@ *