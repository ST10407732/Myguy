@* @model IEnumerable<MYGUYY.Models.TaskRequest>
@{
    ViewData["Title"] = "My Tasks";
}

<h2>My Tasks</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Description</th>
            <th>Status</th>
            <th>Location</th>
            <th>Payment</th>
            <th>Created At</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var task in Model)
        {
            <tr>
                <td>@task.Description</td>
                <td>@task.Status</td>
                <td>Lat: @task.Latitude, Lon: @task.Longitude</td>
                <td>@task.PaymentAmount</td>
                <td>@task.CreatedAt.ToString("g")</td>
            </tr>
        }
    </tbody>
</table>
 *@